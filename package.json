{
  "name": "dev-productivity-mvp",
  "version": "1.0.0",
  "scripts": {
    "install": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build && cd ../backend && npx tsc",
    "start": "cd backend && npm start"
  }
}
